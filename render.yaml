# Render — use Docker (most reliable for TensorFlow)
services:
  - type: web
    name: nrityaai-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
    healthCheckPath: /health
